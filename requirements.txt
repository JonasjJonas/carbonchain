# CarbonChain — dependências Python
# Instalar com: pip install -r requirements.txt
# Para o pipeline de prospecção, usar o venv em prospeccao/

# ── MRV Satélite ──────────────────────────────────────────────────────────────
sentinelhub>=3.9.0          # API Copernicus / Sentinel-2
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# ── Pipeline de prospecção ────────────────────────────────────────────────────
requests>=2.31.0
pandas>=2.0.0
tqdm>=4.65.0

# ── Utilitários ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # leitura do .env

# ── Cartesi dApp ─────────────────────────────────────────────────────────────
# (sem dependências externas além da stdlib)
